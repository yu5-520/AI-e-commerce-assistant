name: PDD
on:
  issues:
    types: [opened, edited]
permissions:
  contents: read
  issues: write
jobs:
  x:
    if: contains(github.event.issue.title, '[自然流]') || contains(github.event.issue.title, '[强付费]') || contains(github.event.issue.title, '[爆品]')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/pdd_operation_analyzer.py result.md
        env:
          ISSUE_TITLE: ${{ github.event.issue.title }}
      - run: