name: Runtime Smoke Test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python -m py_compile src/api/main.py scripts/check_v97.py scripts/smoke_test_runtime.py scripts/smoke_test_api.py
      - run: python scripts/check_v97.py
      - run: python scripts/smoke_test_runtime.py
      - run: python scripts/smoke_test_api.py
