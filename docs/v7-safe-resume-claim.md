# V7 简历安全表述

## 可以写

- 已完成 FastAPI API 原型。
- 已将 Python Mock Workflow 封装为 `/api/demo/run` 接口。
- 已实现前端优先调用 API、失败回退本地样例数据。
- 已实现任务确认 / 拒绝接口原型。
- 已通过 `/api/evals/run` 暴露最小 Evals。

## 不要写

- 已接入真实 ERP / CRM。
- 已接入真实店铺后台。
- 已完成生产级 RPA 自动执行。
- 已完成生产级 Agent 自主运营。
- 已自动处理真实客户触达或退款。

## 推荐表述

> 在 V6 静态前端 Demo 与 Python Mock Workflow 基础上，使用 FastAPI 封装后端接口，实现 `/api/demo/run`、`/api/evals/run` 和任务确认 / 拒绝接口，并改造前端优先调用 API、失败回退本地样例数据，形成前后端可交互的 AI 工作流 Demo。
