# API 大模型接入说明

当前仓库先保留确定性模板：模式识别、文档读取、输出模板、成本/售价/毛利率计算。

复杂运营判断后续交给 API 大模型：

1. 读取 Issue 标题与正文。
2. 判断模式：自然流、强付费、爆品打造。
3. 读取 docs/modules 对应模块文档。
4. 读取 docs/output-templates 对应输出模板。
5. 把输入、模块文档、输出模板发送给大模型。
6. 大模型返回产品化结果卡。
7. GitHub Actions 评论回 Issue。

不建议让 workflow 写业务逻辑。workflow 只负责触发、运行脚本、回写结果。
