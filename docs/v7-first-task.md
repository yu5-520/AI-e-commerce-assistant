# V7 First Task

## 任务

新增 FastAPI 后端，将当前 Python Mock Workflow 封装为 API。

## 文件

```text
src/api/__init__.py
src/api/main.py
requirements.txt
```

## 接口

```text
GET /api/health
GET /api/demo/run
GET /api/evals/run
```

## 注意

只封装当前 Mock Workflow，不接真实 ERP / CRM，不接真实店铺后台。
